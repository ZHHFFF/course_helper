# 课程助手 · 交接文档

> **面向对象**：全权接管本项目开发的接手者（下一个 agent 或协作者）。
> **目标**：读完本文 + `README.md`，**不需要额外沟通**就能独立继续开发。

**接手阅读顺序**：本文档 → `README.md`（功能 / UI 结构 / 踩坑）→
`开发环境搭建说明.md`（本机已装好什么）→ `功能改动说明.md`（相对上游改了什么）。

**当前状态一句话**：可用的测试包，靠 GitHub Actions 出包、发到 Release
（最新 `test-apk-1.2.3-r8`），**未上架应用商店**；核心功能
（签到 / 答题 / 课件缓存 / 保活 / 历史课程爬取）已真机跑通。

**最近在做什么**（截至 2026-09-24）：刚完成**雨课堂爬虫**，
支持回看已结课课程的活动与课件（见 §3.1）。

---

## 1. 项目目标与背景

### 是什么

一个 **Android 课堂助手 App**（Flutter），同时支持两个国内高校教学平台：

| 平台 | 代号 | 说明 |
|------|------|------|
| 学习通（超星） | `chaoxing` | 签到、活动、IM 通知 |
| 雨课堂 | `rainClassroom` | 上课看 PPT、课堂答题 |

雨课堂还分 4 个服务器（`lib/platform.dart` 的 `RainClassroomServerType`）：
`yuketang` / `pro`(荷塘) / `changjiang`(长江) / `huanghe`(黄河)。
不同服务器的 WebSocket 域名不同，**写死逻辑前先确认当前是哪个服务器**。

### 解决什么问题

上课时的高频痛点：自动签到、课堂题自动作答、PPT 课件离线缓存、
后台保活（切后台/锁屏后仍能收到发题和签到提醒）。

### 上游与 fork 关系

- `origin` = `ZHHFFF/course_helper`（同学的仓库，**CI 跑在这里**）
- `userfork` = `makisekurse/course_helper`（自己的 fork，也发 release）
- 两个远端都能推（凭据在 Windows 凭据管理器的 `git:https://github.com`）

### 本 fork 做了什么

见 `功能改动说明.md`。主线是：**UI 全量迁移到 Miuix**（小米 HyperOS 设计语言，
`flutter_miuix` 包）、新增「课件」Tab、新增 AI 答题检索、加前台服务保活。

---

## 2. 代码结构与模块

`lib/` 共 **82 个 dart 文件**，按职责分层：

```
lib/
├── main.dart              # 入口：初始化顺序、4 Tab 骨架、玻璃底栏
├── platform.dart          # 平台/服务器枚举与切换
├── api/                   # 13 个：一个平台能力一个文件
│   ├── api_service.dart   #   统一请求出口（dio + cookie + 多账号并发）
│   ├── course.dart        #   雨课堂：课程/PPT/答题/签到（★ 见 §3）
│   ├── rc_crawler.dart    #   ★ 雨课堂爬虫：历史结课课程的活动与课件（见 §3.1）
│   ├── answer_search.dart #   AI 检索（★ 最复杂，提示词 + 解析）
│   ├── sign_in.dart       #   学习通签到（普通/位置/手势/二维码/拍照）
│   ├── login.dart         #   两平台登录（含腾讯验证码、微信扫码）
│   └── quiz/evaluate/active/face/image/topic_discuss/captcha.dart
├── models/                # 数据模型（answer_result / presentation / course / active / rc_activity）
├── pages/                 # 页面
│   ├── presentation.dart  #   雨课堂上课页（★ 2900 行，最核心最复杂）
│   ├── courses/           #   课程列表 + 课程内容
│   ├── courseware/        #   课件列表 + 离线浏览
│   ├── actives/           #   学习通活动（签到/测验/问卷/投票/讨论/评分）
│   ├── settings/          #   设置（外观/关于）
│   ├── widget/            #   ★ 可复用组件集中地，改 UI 先来这找
│   ├── accounts.dart      #   多账号管理
│   └── login.dart
├── cache/                 # 三级缓存：答案 / PPT / 课程
├── session/               # 账号、cookie 管理
├── setting/               # 用户设置（外观 / 自动答题）
├── utils/                 # ★ 纯逻辑集中地（可单测）
└── push/                  # 环信 IM（学习通活动推送）
```

### 页面结构（4 Tab）

底栏由 `main.dart` 的 `Stack` 承载，**不是** `Scaffold.bottomNavigationBar`：

| # | Tab | 页面 | 保活 |
|---|-----|------|------|
| 0 | 课程 | `pages/courses/list.dart` | ✅ `Offstage`（3 秒轮询不能断） |
| 1 | 账号 | `pages/accounts.dart` | ❌ 切走即释放 |
| 2 | 课件 | `pages/courseware/list.dart` | ✅ `Offstage`（要记住打开到哪一层） |
| 3 | 设置 | `pages/settings/settings.dart` | ❌ 切走即释放 |

---

## 3. 关键文件与代码入口

### 必须知道的几个

| 文件 | 作用 | 注意 |
|------|------|------|
| `main.dart` | 入口、初始化顺序、Tab 骨架、`_MiuixScope` | `MiuixTheme` 必须包在 `MaterialApp.builder`；`_GlassNavInsets` **只能挂 `home:`**，挂 builder 会污染所有二级页 |
| `pages/presentation.dart` | 雨课堂上课页，**2900 行** | 自动答题全流程都在这；WebSocket、预选、自动提交、课件缓存 |
| `api/course.dart` | 雨课堂所有接口 | `answer()` 的 `Content-Type` 是坑，见 §8；`checkIn()` 有三个崩溃点已修，见 §3.1 |
| `api/rc_crawler.dart` | ★ 雨课堂爬虫（历史结课课程） | 多路兜底探测，见 §3.1 |
| `models/rc_activity.dart` | 教学活动模型（`RCActivity`） | `parseActivitiesJson()` 是纯函数，有测试 |
| `api/answer_search.dart` | AI 检索 | 提示词 + JSON 解析；`_readAnswerKeys` / `parseAIResponse` |
| `models/answer_result.dart` | 答案模型 | `matchOptionKeys()` 把答案文本映射到选项 key |
| `utils/answer_filling.dart` | 「写哪个字段」的纯逻辑 | 不同题型读不同字段，写错的表现是「填了等于没填」 |
| `utils/keep_alive_service.dart` | 前台服务 | 只负责保进程，真正收消息的 WebSocket 在主 isolate |
| `pages/widget/miuix_card_metrics.dart` | ★ 卡片内边距常量 | 见 §8 的 MiuixCard 坑 |
| `pages/widget/miuix_nav_metrics.dart` | 底栏几何契约 | 「底栏顶边离屏底多远」的**单一事实来源** |

### 自动答题数据流（改动这块前必读）

```
老师发题
  └─ 雨课堂 WebSocket 推 unlockproblem（prob / limit / dt）
       └─ pages/presentation.dart  _handleMessage → case 'unlockproblem'
            └─ _maybeAutoSubmit(prob)          # 入队，串行处理
                 └─ _doAutoSubmit(prob)
                      1. _waitForProblemSlide  # 定位并切到题目页
                      2. _waitForAnswer        # 等 AI 检索结果（最多 25s，受剩余答题时间限制）
                      3. _fillAnswerSilently   # 按题型写 _answer / _textAnswer
                      4. randomDelay           # 拟人化延迟
                      5. _submitAnswer         # → _submitForAllAccounts → api.answer
```

**两条独立来源**：
- **预选**：`_autoSelectIfCurrent`（答案一到就填进作答区，只填不交）
- **提交**：`_doAutoSubmit`（老师发题后才触发）

两者都受 `AutoAnswerSetting` 三个开关控制（自动检索 / 自动预选 / 自动提交）。

### 3.1 雨课堂爬虫：历史结课课程的活动与课件

**业务背景**：雨课堂的**已结课/已归档**课程，移动端接口拿不到活动列表和课件。
用户想回看以前课的 PPT，就得走 Web 端接口。这块逻辑集中在
`api/rc_crawler.dart`（511 行）+ `models/rc_activity.dart`（311 行），
由课件页（`pages/courseware/list.dart`）调用。

**核心难点：历史课程没有「单一可靠」的 presentationId 来源**，
所以用**多路兜底探测**，任一命中即用：

| 链路 | 接口 | 说明 |
|------|------|------|
| 活动自身 | — | `RCActivity.presentationIds`（从 `content` / `res_list` 里抠） |
| A | `/api/v3/lesson-summary/student` | 学生历史报告 |
| B | `/v2/api/web/lessonafter/{lessonId}/presentation` | Web 端课后课件 |
| C | `/api/v3/classroom-report/student/lesson-info` | 课堂报告 |
| D | 实时课堂 WebSocket 握手 | 需 `lessonToken`，超时 3~4s |
| 兜底 | — | 把 `lessonId` 本身当 presentationId 试 |
| 卡片 | `/v2/api/web/cards/detlist/{coursewareId}` | Type 2 课件资料，**构造**出 slides |

拿到候选 ID 后，拉 PPT 元数据也走三级降级：
`/api/v3/lesson/presentation/fetch` → `/api/v3/lesson-summary/student/presentation`
→ `/v2/api/web/lessonafter/presentation/{id}`，成功即 `PptCache.save` 落盘。

**课程列表的三路合并**（`RCCrawler.mergeCourses`，纯函数）：
移动端学习列表（信息最全）＋ Web 在修课程 ＋ 已归档课程，
按 `classId`/`courseId` 去重，结课课程打 `state: false` + note 追加 `[已结课]`。
排序规则：**正在上课 > 进行中 > 已结课**。

**⚠️ `checkIn()` 的三个崩溃点**（`651637f` 已修，别再写回去）：

历史结课课程调 `/api/v3/lesson/checkin` 时服务器**不返回 `set-auth` 头**，
原代码有三处必崩：

```dart
final int code = data['code'];                            // ① 非空类型转换，字段缺失就抛
final bearerToken = response.headers.value('set-auth')!;   // ② ! 断言，头不存在就抛
final lessonToken = data['data']['lessonToken'];          // ③ 链式下标，data['data'] 为 null 就抛
```

现在全部加了守卫（`data is Map ? … : null` / `?? ''` / `?.`），
并且**只在真有值时才调 `_setToken`**。

`checkIn(lessonId, {classroomId})` 的两种形态：
- 传 `classroomId` → 历史课程形态，body 只发 `{lessonId, classroomId}`
- 不传 → 实时课堂形态，body 发 `{lessonId, source: 21, joinIfNotIn: true}`

两种都带 `xtbz: ykt` + `content-type: application/json` 头。

**相关测试**：`test/rc_crawler_test.dart`（17 条），覆盖 `extractCoursesList` /
`mergeCourses` / `parseActivitiesJson` 这几个纯函数。

---

## 4. 技术栈与依赖

### 工具链（本机已装好，见 `开发环境搭建说明.md`）

| 组件 | 版本 | 路径 |
|------|------|------|
| Flutter | 3.47.5（Dart 3.13.4） | `D:\dev\flutter` |
| Temurin JDK | 17 | `D:\dev\jdk17` |
| Android SDK | platform 37 / build-tools 37.0.0 | `D:\dev\android-sdk` |

项目要求：`compileSdk = 37`、Gradle 8.14.5、AGP 8.x、`minSdk 24`、`targetSdk 36`。

### 关键依赖

| 包 | 用途 |
|----|------|
| `flutter_miuix ^1.2.0` | ★ UI 框架（HyperOS 设计语言），所有规范按它 |
| `dio ^5.9.1` | HTTP（★ 编码行为有坑，见 §8） |
| `flutter_foreground_task ^10.0.0` | 前台服务保活 |
| `im_flutter_sdk ^4.19.1` | 环信 IM（学习通活动推送） |
| `sqflite` / `shared_preferences` / `flutter_cache_manager` | 存储 |
| `flutter_baidu_mapapi_*` / `flutter_bmflocation` | 位置签到 |
| `mobile_scanner` / `camera` | 扫码签到 |
| `pdf` / `image` / `share_plus` | PPT 导出 PDF、日志导出 |

⚠️ **`flutter_tencent_captcha` 是 git 依赖**（`AneryCoft/flutter_tencent_captcha`）。
本机跑验证脚本时会把它换成 `path: D:/dev/vendor/flutter_tencent_captcha`
（因为连不上 github.com），**别把这个改动提交上去**。

---

## 5. 运行与部署

### 开发运行

```bash
cd D:/workbuddy工作空间/course_helper
flutter pub get          # 需要代理才能到 pub.dev
flutter run
```

### 出包：交给 CI，不要本地构建

```bash
git push origin HEAD:master
```

CI 触发分支：`master` / `main` / `makisekurisu`（推开发分支也能出包）。

**版本号是自动的**：`BUILD_NUMBER = ${{ github.run_number }}`，只增不减 ——
新包才能覆盖安装旧包。本地手打版本号很容易打低，装的时候会被系统拒
（「应用未安装」）。

⚠️ **`--split-per-abi` 会给 arm64 的 versionCode 多加 2000**：
CI 里 `--build-number=52` 在真机上读出来是 **2052**。
想确认某个包的真实 versionCode：

```bash
aapt2 dump badging app.apk | head -1
```

### CI 构建耗时（2026-09-24 优化，别再退回）

`Build release APK` 原来是整个 run 的瓶颈：**9.48 / 11.0 分钟（88%）**。

| run | 说明 | Build 步骤 | run 总时长 |
|-----|------|-----------|-----------|
| 56 | 基线 | 9.48min | 11.0min |
| 59 | **优化后稳态** | **4.53min** | **6.4min** |

**手段只有两处配置**（无代码改动）：
- `.github/workflows/build-apk.yml`：`setup-java` 的 `cache: 'gradle'`
  → 缓存 `~/.gradle/caches` + wrapper（Gradle 的 build-cache-1 也在里面）
- `android/gradle.properties`：`org.gradle.caching=true`、`org.gradle.parallel=true`

⚠️ **`android.enableJetifier` 必须保持 `true`**，关掉会构建失败
（`:app:checkReleaseDuplicateClasses` 报 `android.support.v4.*` 重复类）。
根因是传递依赖 `flutter_baidu_mapapi_map` → `android.arch.lifecycle:extensions:1.1.1`
→ `com.android.support:support-compat:26.1.0`。
**排查教训**：只 grep 包名 `android.support.` 查不出来，必须 grep Maven 坐标
`com.android.support:`，或直接翻各插件 android/build.gradle 的 implementation 行。

**还想再快的话**（未做，按收益排序）：
1. `org.gradle.configuration-cache=true` —— 可能再省配置阶段，但与 Flutter
   gradle 插件兼容性需实测
2. 换更大的 runner（付费）—— 构建是 CPU 密集，核多直接快
3. 缩减 APK 体积 —— 但实测大头全是核心依赖（百度地图 18.3MB、dex 13.4MB、
   Flutter 引擎 11.2MB、Dart AOT 10.7MB、环信 IM 8.9MB、MLKit 4.7MB），动不了

### 签名（重要）

CI 用仓库 Secret `TEST_KEYSTORE_BASE64` 里的**固定**测试密钥签名。
`.gitignore` 排除了 `*.jks` / `key.properties` → **本地拿不到同一把密钥**。

**所以：不要本地出包。** 签名不同 → 用户装不上去（签名冲突，得先卸载、丢数据）。

Secret 没配时 CI 会现场生成临时密钥（包能出，但签名每次都变，装前要先卸载）。

### 发布到 Release

`D:\dev\publish.py`（本地脚本，不在仓库里）：

```bash
printf 'protocol=https\nhost=github.com\n\n' | git credential fill 2>/dev/null \
  | grep '^password=' | cut -d= -f2- > /tmp/ghtok
cd /d/dev && python -u publish.py "$(cat /tmp/ghtok)" <完整SHA> <tag>
```

它会：校验 CI 构建的 sha 是否匹配 → 下载 artifact → 建 release（两个仓库）→
上传 APK → 回查附件。

**下载用并行分块**（签名 URL 支持 Range，8 块并行实测 156 秒，
单流要 12 分钟）。细节和踩过的坑见 skill `github-ci-artifact-release`。

**tag 命名约定**：`test-apk-<版本>-r<N>`，`N` 递增。
**tag 里的 rN 和 CI 的 run_number 不是一回事**，别混：

| tag | 对应 commit | CI run | 内容 |
|-----|------------|--------|------|
| `…-r5` | — | 49 | 更早的版本 |
| `…-r6` | `be15ba8` | 52 | 文字对齐 / 多选 / 保活三修 |
| `…-r7` | `3141d1b` | 54 | 雨课堂爬虫初版 |
| `…-r8` | `651637f` | 56 | 历史课件抓取修复（**最新**） |

发新包前先看现有 tag 定下一个 `rN`：

```bash
curl -s -H "Authorization: Bearer $TOKEN" \
  "https://api.github.com/repos/ZHHFFF/course_helper/releases?per_page=5" \
  | python -c "import sys,json;[print(r['tag_name']) for r in json.load(sys.stdin)]"
```

> ⚠️ 发**纯文档/注释**的提交也会触发一次 CI 构建并顶掉 `run_number`
> （→ 版本号自增）。如果只是为了留档，先问用户要不要发，别顺手推。

---

## 6. 测试与验证

### 本地验证脚本（本机专用，不在仓库里）

```bash
bash /d/dev/check.sh        # flutter analyze
bash /d/dev/check.sh test   # analyze + flutter test
```

⚠️ **它为什么要存在**（两个环境坑，不处理的话一条测试都跑不起来）：

1. **项目路径含中文会让 analysis server 崩** → 脚本先把源码同步到纯 ASCII 路径
   `D:/dev/course_helper_check` 再跑。
2. **`flutter test` 会被代理破坏**：它会起 `flutter_tester` 进程并用 WebSocket
   连**本机回环**，而环境的 `http_proxy=127.0.0.1:3017` 把回环连接也代理掉了，
   所有测试报 `Invalid WebSocket upgrade request`。
   → 跑测试必须**摘掉代理**（脚本里做了）；但 `pub get` 反过来**要用代理**。
3. Windows 上 Flutter 要读 `%PROGRAMFILES(X86)%` 定位 Visual Studio，
   缺了直接 `throwToolExit`（脚本里用 `env` 注入）。

### 测试规模

**20 个测试文件、300+ 条用例**（`rc_crawler_test.dart` 之后新增），CI 上全绿。

覆盖方式：**把易错逻辑抽成不依赖 Flutter 的纯函数再测**，例如

| 纯函数 | 文件 | 防的是什么 |
|--------|------|-----------|
| `AnswerFilling.fieldFor` | `utils/answer_filling.dart` | 题型写错字段 → 「填了等于没填」 |
| `isCurrentProblemPublished` | `utils/problem_publish.dart` | 两套 ID 命名空间对不上 → 提交按钮永不出现 |
| `pickResyncProblemId` | `utils/problem_publish.dart` | 断线补查误交历史题 |
| `QuestionHash.of` | — | 题目指纹 |
| `RCCourseApi.buildAnswerBody` | `api/course.dart` | 多选答案被编码展平 |
| `Course.sameShallowAs` | `models/course.dart` | 轮询误判「数据变了」→ 整页闪 loading |
| `RCCrawler.extractCoursesList` | `api/rc_crawler.dart` | 三种响应嵌套形态（`data.list` / `data` / 裸数组） |
| `RCCrawler.mergeCourses` | `api/rc_crawler.dart` | 三路课程合并去重、结课标记、排序 |
| `RCActivity.parseActivitiesJson` | `models/rc_activity.dart` | 活动类型/课件 ID 提取 |

**新增功能时优先照这个模式做**：把判定逻辑抽成纯函数 → 写单测 → 页面只做状态搬运。

### 真机验证清单

`analyze` + `test` 只能证明「没编译错、纯逻辑对」，**UI 和系统行为必须真机验**：

- [ ] 底栏 4 Tab 切换、选中态、滚动时模糊是否实时跟随
- [ ] 课件页三层（课程 → 课件列表 → 离线浏览）+ 逐份删除
- [ ] 设置页各入口 + 深浅色切换
- [ ] 各页面卡片内边距是否一致（见 §8 的 MiuixCard）
- [ ] 多选题是否记录全部选项
- [ ] 挂后台 20 分钟以上，是否还能自动提交
- [ ] **历史结课课程**能列出活动、能抓到课件（见 §3.1）

> 💡 **用户通常自己跑真机验证**，不要反复催他跑本地 `check.sh` ——
> 他明确说过不用。本地验证只在你自己改完代码、想确认没编译错时才跑。

---

## 7. 待办与已知问题

### 待办

1. ⚠️ **真机回归**：§6 那份清单还没全验完（用户会自己跑，别催）
2. `lib/pages/presentation.dart`（2900 行，改之前先通读 §3 的数据流）
3. **底栏折射（方案 D）**：`ImageFilter.shader`，**仅 Impeller 可用**
4. **应用被锁 60Hz**（`frameRateOverride uid=10196`），需 Android 侧
   `Surface.setFrameRate` / `preferredDisplayModeId` 或 ColorOS 白名单
5. 老缓存的课程名回填（进一次那门课即可）
6. **课件页 `list.dart` 已 1469 行**，是下一个该拆分的候选
   （现在混了：课程列表 / 活动列表 / 课件列表 / 爬取进度 / 删除面板 5 种职责）

### 已知问题

- **文字对齐**：根因已定位并修复（`MiuixCard` 内边距，见 §8），
  但仍建议在新包上复看一眼
- **历史结课课程的课件抓取**依赖多路兜底（§3.1），
  雨课堂接口一改就可能整条链路失效 —— 排查时先看 `RCCrawler` 的日志 tag
- 应用内「检查更新」拉的是 GitHub Releases 列表；本项目全是 prerelease，
  所以**不能用 `/releases/latest`**（那个接口不返回 prerelease，实测 404）

---

## 8. 风险点与注意事项（接手必读）

### ⛔ 删除红线

**绝不在用户没有逐项点头的情况下删除任何东西。**

本环境有 `codebuddy-safe-delete` 钩子：**任何删除 API 都会被重定向到回收站**，
没有「直接永久删除」的普通途径。相关表现：

- `[System.IO.File]::Delete()` 也会进回收站（**不能**绕过钩子）
- 在 `D:\` 根下 `trash` 操作本身会失败并抛 `OSError`（删不掉）
- `SendToRecycleBin` 会**假报错但实际成功**（判断成败要看 `Test-Path`，别信异常）
- 结论：**要删就先列清单 → 问 → 得到明确同意才动手**。
  「继续」「全权解决」**不等于**「可以删」。

### ⚠️ 代码层面最容易踩的坑

1. **`MiuixCard` 的 `insideMargin` 默认是 `EdgeInsets.zero`（0）**，
   而各 preference 是 16（`MiuixBasicComponentDefaults`）。
   裸用 `MiuixCard` 包裸内容 → 文字紧贴卡片边缘 → 和带 preference 的卡片并排
   就是「没对齐」。
   **凡是裸 `MiuixCard` 都必须显式传 `kMiuixCardInsideMargin`**
   （`pages/widget/miuix_card_metrics.dart`）。
   反例：卡片里已有 preference 或显式 `Padding` 时**不要加**（会双重缩进）。

2. **`dio` 发 `Map` body 时若不设 `Content-Type`，会被编成
   `application/x-www-form-urlencoded`** → 列表值被展平成重复参数
   （`result: ['A','B']` → `result=A&result=B`），服务端只取第一个。
   **body 里有列表就必须显式设 `'Content-Type': 'application/json'`**。
   （`api/course.dart` 的 `answer()` 就是这么踩的，导致多选题只提交一个选项）

3. **不要跑 `dart format`**：本项目没用它，一次重排 531 行，会造成大面积合并冲突。

4. **不要污染 `MediaQuery.viewPadding`**：底栏靠它算手势区占位高度，
   污染会导致底栏整体被顶高。要补偿 SnackBar 只改 `padding`。

5. **保活 isolate 和主 isolate 的静态状态是两套**：
   保活 isolate 里直接调 `AppLogger` 只会写进没人读的内存队列，
   必须 `FlutterForegroundTask.sendDataToMain` 交给主 isolate 记。

6. **`unlockproblem` 是推送，断了就永久丢**：
   光重连补不回事件，所以 `_connectWebSocket` 里带了
   「重连后从 timeline 补查」的逻辑。改 WebSocket 那块时别把它删了。

### ⚠️ 本机环境的特殊性（影响你干活的方式）

- **默认用 PowerShell 7**（`C:\Program Files\PowerShell\7\pwsh.exe`），
  用完整路径调用（PATH 是启动时快照，裸写 `pwsh` 会静默失败）
- Bash 工具**禁止**调 `pwsh`，PowerShell 逻辑只能在 PowerShell 工具里跑
- 沙箱拦截 `wmic.exe` / `reg.exe` / `fsutil` / `net` / `whoami`
- PowerShell 工具的 stdout 不回显时，**写文件再读**
- 工作空间在 **D 盘**（`D:\workbuddy工作空间\`），**用户不希望文件落在 C 盘**

### ⚠️ 发包相关的风险

- **本地构建会因签名不同导致无法覆盖安装** → 别本地出包（见 §5）
- 发布前**必须断言 CI 构建的 sha == 要发布的 sha**，否则会发错版本
- 建完 release **必须回查 `assets`**：上传偶尔会「release 建好了但附件没上去」
  且返回空字符串，不回查就会以为成功了

---

## 9. 快速上手：第一次接手建议做什么

1. 读本文 + `README.md`，跑一次 `bash /d/dev/check.sh test` 确认环境能跑
2. 拉最新代码，看 `git log --oneline -20` 了解最近在改什么
3. 装一个最新 release 到真机，按 §6 的清单过一遍
4. 挑一个待办（建议从「底栏折射」或「60Hz」入手，边界清晰）
5. 改完：`check.sh test` 通过 → 推 `master` → 等 CI → `publish.py` 发 release
   → 真机验

**有疑问时的判断依据**：代码注释密度很高，**几乎每个反直觉的决定都写了「为什么」**
（尤其是「踩过的坑，别改回去」这类）。改之前先把那段注释读完，能省很多时间。
